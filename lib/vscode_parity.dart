/// The complete typed VS Code Parity Layer (ADR 0012).
///
/// Mechanically generated from the pinned `vscode.d.ts` by Total Mapping
/// Rules — a faithful typed mapping of every public capability, rooted in
/// the module object an extension receives at activation (`VscodeApi`).
/// The Idiomatic Facade grows over this layer incrementally.
library;

export 'src/generated/vscode_parity_layer.g.dart';
