# Architecture

The living architecture documentation for `flutter_vscode` — module
boundaries, the binding pipeline, host execution rules, the view
protocol, the author workflow, and the evidence chain — lives in
[`specs/architecture/`](../../specs/architecture/index.md), kept
current by the repository's prose-truth gates.

This page is a pointer so existing links keep resolving; nothing
architectural is documented here.
