# Backlog

The discovery inbox. Findings made during a frozen round land here
instead of growing that round's exit bar, then graduate to one of:

- the active named plan — currently
  [`parity-layer.md`](parity-layer.md) (the complete typed Parity
  Layer, ADR 0012);
- [`futures.md`](futures.md) — deferred product work, engineering debt,
  and the upstream-contribution stance;
- a new named plan under `specs/plans/` with its own machine-checked
  exit bar.

Nothing in this file blocks a round's closure.

## Open discoveries

(none — everything previously here has graduated to the files above)
