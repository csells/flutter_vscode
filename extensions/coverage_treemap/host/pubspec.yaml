name: coverage_treemap_host
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  coverage_treemap_shared:
    path: ../shared
  flutter_vscode:
    path: ../../../packages/flutter_vscode

dev_dependencies:
  very_good_analysis: ^10.0.0
