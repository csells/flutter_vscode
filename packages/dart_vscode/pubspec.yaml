name: dart_vscode
description: >-
  The complete typed Dart mapping of the VS Code extension API, with a
  Dart-first ergonomic surface. Pure Dart, no Flutter dependency.
version: 0.1.0
homepage: https://github.com/SlowGen/flutter_vscode
repository: https://github.com/SlowGen/flutter_vscode
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  meta: ^1.9.1

dev_dependencies:
  path: ^1.8.3
  test: ^1.24.0
  very_good_analysis: ^10.0.0
